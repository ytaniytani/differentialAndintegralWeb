# 仕様書:『ゲームCGで体感する微分・積分』インタラクティブ学習ページ

- リポジトリ: `differentialAndintegralWeb`
- バージョン: 1.0(初版)
- 目的の一言まとめ: **「微分・積分って何の役に立つの?」に、ゲームのリアルタイムCGの実例を触りながら答えるWebページ**

---

## 1. コンセプト

### 1.1 誰のためのページか

- 微分・積分を「習ったけど表面的にしか知らない」人
- ゲームやCGに興味があり、「数学がどこで使われているか」を知りたい人
- 数式の変形よりも「意味」と「使いどころ」を直感で掴みたい人

### 1.2 第一のテーマ

**「便利さの体験」を最優先にする。** 証明や厳密な定義は扱わない。
すべてのセクションで次の3点セットを守る:

1. **ゲームCGの具体的な場面**から始める(キャラのジャンプ、カメラ移動、ライティング…)
2. **スライダーや数値入力を触ると、グラフとCGプレビューがリアルタイムに動く**
3. 触った結果として「微分/積分がないとこれは作れない」と**体で納得**して終わる

### 1.3 デザイン方針

- 1ページ縦スクロール構成。上から順に読む/触ると、微分 → 積分 → 応用と自然に進む
- 各セクションは「① 場面の説明(2〜3文) → ② インタラクティブデモ → ③ ひとことまとめ」の型で統一
- 数式は最小限。出すときは必ず「日本語の読み下し」を併記する
  - 例: `v = dx/dt` →「速度は、位置が1秒あたりどれだけ変わるか」
- ダークテーマ基調(ゲーム/CGツールらしい見た目)。グラフ・プレビューは高コントラストで

---

## 2. ページ全体構成

```
ヘッダー(タイトル + キャッチコピー)
│
├─ §0 イントロ:このページの遊び方
│
├─ 【微分編】
│   ├─ §1 微分=「今この瞬間の変化」 … キャラの移動と速度
│   └─ §2 微分でなめらかを作る      … イージング(アニメーションカーブ)
│
├─ 【積分編】
│   ├─ §3 積分=「積み重ねて元に戻す」 … ジャンプの物理(重力の積分)
│   └─ §4 積分=「面積を求める」       … 速度グラフの下の面積 = 移動距離
│
├─ 【リアルタイムCGならではの話】
│   ├─ §5 Δt とフレームレート … 数値積分の誤差を目で見る
│   └─ §6 微分が作る「向き」と「光」 … 曲線の接線と地形の法線・ライティング
│
└─ フッター(まとめ + 用語ミニ辞典)
```

- 全6デモ。各デモは独立して動く(順番に読まなくても壊れない)
- ページ上部に固定ナビ(セクションへのジャンプリンク)

---

## 3. 各セクション詳細仕様

> 共通UI仕様:
> - スライダーは操作中もリアルタイム(input イベント)でグラフ・プレビューを更新
> - 数値は現在値をスライダー横に常時表示。数値入力欄と双方向同期
> - 各デモに「リセット」ボタン
> - グラフはマウスホバーで座標値をツールチップ表示

### §0 イントロ

- 内容: 「このページは読むより触るページです」という宣言と、微分・積分の超ざっくり定義
  - 微分 = **変化の速さを取り出す道具**
  - 積分 = **変化を積み重ねて合計を出す道具**
  - ゲームは毎秒60回この2つを回して動いている、という導入
- インタラクション: 小さなデモとして、跳ねるボールのループアニメを配置(触れなくてよい、雰囲気づくり)

### §1 微分=「今この瞬間の変化」— キャラの移動と速度

**場面**: 横スクロールゲームのキャラが右に移動する。ゲームでは毎フレーム「今の速度」が必要(移動、アニメ切替、エフェクトの強さなどに使う)。

**画面構成**(左右2ペイン):
- 左: **CGプレビュー** — 地面の上をキャラ(単純な矩形+目のスプライトで可)が位置 x(t) に従って動く
- 右: **グラフ** — 横軸 t、縦軸 x の位置グラフ。現在時刻に接線を重ね描きし、接線の傾き = 速度を数値表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 時刻 t | スライダー | 0〜5s | キャラとグラフ上の点・接線が連動して動く |
| 再生/停止 | ボタン | - | t を自動で進める |
| 動き方 | 選択 | 等速 / 加速 / 減速して止まる / 行って戻る | x(t) の関数を切替 |
| Δt(差分幅) | スライダー | 1.0 → 0.001s(対数) | (x(t+Δt) − x(t)) / Δt の割線を表示。狭めると接線に一致していく様子を見せる |

**体験させたいこと**:
- 「速度=位置グラフの傾き」を、接線とキャラの実際の速さの連動で実感する
- Δt を小さくしていくと割線→接線になる = **微分の定義そのもの**を、式ではなく操作で理解する
- 「行って戻る」を選ぶと、折り返し点で速度0・その後マイナスになるのが見える

**まとめ文言案**: 「ゲームエンジンが毎フレームやっているのは、まさにこの Δt を小さくした引き算。微分は『今の勢い』を取り出す道具。」

### §2 微分でなめらかを作る — イージング(アニメーションカーブ)

**場面**: UIパネルのスライドインやカメラ移動。線形補間(lerp)だと機械的で安っぽい。プロは「イージングカーブ」を使う。カーブの良し悪しは**微分(速度)の連続性**で決まる。

**画面構成**:
- 左: **CGプレビュー** — パネル(またはカメラに追従する四角)が0→1へ移動するアニメをループ再生
- 右: **グラフ2段** — 上段: 補間カーブ f(u)、下段: その微分 f′(u)(速度カーブ)。微分カーブは自動計算して表示

**操作**:
| コントロール | 種類 | 効果 |
|---|---|---|
| カーブ選択 | ボタン群 | linear / easeInQuad / easeOutCubic / smoothstep / easeOutBounce |
| 再生速度 | スライダー | アニメの所要時間 0.2〜3s |
| 「速度カーブを見る」 | トグル | 下段グラフの表示ON/OFF |

**体験させたいこと**:
- linear は速度カーブが**両端で不連続**(いきなり動き、いきなり止まる)→ プレビューでカクッと感じる
- smoothstep は速度が0から始まり0で終わる → 高級感のある動きになる
- **「気持ちいい動き」の正体は微分カーブの形**だと気づかせる

**まとめ文言案**: 「アニメーターやテクニカルアーティストはカーブの微分を見て動きを調整している。微分は『なめらかさの品質チェック』の道具。」

### §3 積分=「積み重ねて元に戻す」— ジャンプの物理

**場面**: ゲームのジャンプ。プログラムが知っているのは「重力加速度」だけ。そこから毎フレーム、加速度→速度→位置へと**積み上げる(積分する)**ことでジャンプ軌道が生まれる。

**画面構成**:
- 左: **CGプレビュー** — キャラがジャンプ。軌道を残像(点線)で描画
- 右: **グラフ3段連動** — 上から 加速度 a(t) / 速度 v(t) / 高さ y(t)。現在時刻のラインが3つのグラフを貫く

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 重力 g | スライダー | 1〜30 m/s² | 月面ジャンプ〜重い星。軌道が即時再計算 |
| 初速(ジャンプ力) | スライダー | 2〜15 m/s | 軌道の高さ・滞空時間が変わる |
| ジャンプ実行 | ボタン | - | アニメ再生 |
| 空中制御(2段ジャンプ) | ボタン | - | 空中で速度を上書き→軌道が折れる。積分は「その瞬間からの積み上げ」だと見せる |

**体験させたいこと**:
- 「加速度を1回積分すると速度、もう1回積分すると位置」の**積分2連チェーン**を3段グラフの連動で見る
- 重力スライダーを動かすと放物線が即座に変形 → パラメータ調整でゲームの「手触り」を設計できる
- 微分と積分が**逆演算**であること(§1の逆向き)に気づかせる

**まとめ文言案**: 「マリオのジャンプの気持ちよさは、重力と初速という2つの数値の積分で出来ている。積分は『ルールから動きを生み出す』道具。」

### §4 積分=「面積を求める」— 速度グラフの下の面積 = 移動距離

**場面**: 「このキャラ、結局どれだけ進んだ?」— 速度が刻々と変わるとき、合計移動距離は速度グラフの**下の面積**。ゲーム内のダメージ蓄積(DPS×時間)やゲージの溜まり量も同じ計算。

**画面構成**:
- 中央: **グラフ** — 速度 v(t) の曲線。t=0〜現在まで の領域を短冊(リーマン和の長方形)で塗る
- 下: **CGプレビュー** — 実際にキャラがその速度で移動し、進んだ距離をメーター表示。短冊の合計面積の数値と並べて表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 短冊の本数 N | スライダー | 1〜200 | 長方形近似が細かくなり、面積の数値が真の距離に収束していく |
| 速度カーブ | 選択 | 一定 / 加速 / 波打つ(sin) | 題材の切替 |
| 区間の終端 t | スライダー | 0〜10s | 塗られる範囲と移動距離が連動 |

**体験させたいこと**:
- N=1(ガタガタ)→ N=200(ほぼ曲線)で、**近似誤差がゼロに近づく**のを面積の数値と併せて見る
- 「キャラが実際に進んだ距離」と「面積の数値」が一致する瞬間の納得感
- 短冊 = ゲームの1フレーム分の移動、という §5 への伏線

**まとめ文言案**: 「積分記号 ∫ は『細かく刻んで全部足す』の合図。ゲームは毎フレームこの短冊を1本ずつ足している。」

### §5 Δt とフレームレート — 数値積分の誤差を目で見る

**場面**: 同じジャンプのコードでも、30fpsと144fpsで軌道が変わってしまうことがある。リアルタイムCGは「連続な数学」を「飛び飛びのフレーム」で近似しているから。**これは §1〜§4 の知識が実務に直結する瞬間。**

**画面構成**:
- 中央: **軌道比較ビュー** — 同一のジャンプを、理論解(解析積分・滑らかな線)とオイラー法(選んだfpsで点をつなぐ)で重ね描き
- 誤差(最高到達点の差など)を数値で表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| フレームレート | スライダー | 5〜240 fps | 点の間隔と誤差がリアルタイムに変化 |
| 積分方法 | 選択 | オイラー法 / セミインプリシット・オイラー法 | ゲームエンジン標準の後者で誤差が減るのを見せる |
| スロー再生 | トグル | - | 1フレームずつの「速度を足す→位置を足す」を強調表示 |

**体験させたいこと**:
- fps=5 だと軌道がガタガタで理論解から大きくズレ、fps=240 でほぼ一致する
- 「fpsが低い環境でジャンプの高さが変わるバグ」がなぜ起きるかを理解する
- 微積分を知っている人は誤差の原因を説明でき、対策(積分法の選択、固定タイムステップ)を打てる — **知識の実利**を示す

**まとめ文言案**: 「UnityもUnrealも中身は数値積分。微積分は『ゲームエンジンの中身を読める言葉』。」

### §6 微分が作る「向き」と「光」 — 接線・法線・ライティング

**場面**: 2つの実例を1セクションで:
1. **パス移動** — レールに沿って動くカメラや敵キャラは、曲線の**微分(接線)**で進行方向を向く
2. **ライティング** — 地形の明るさは、表面の傾き(微分)から作る**法線**と光の向きで決まる(ランバート反射)

**画面構成**(タブ切替で2デモ):

タブA「パス移動」:
- ベジェ曲線上を矢印(またはカメラアイコン)が移動。接線ベクトルを矢印で表示
- 操作: 曲線の制御点4つをドラッグで移動 / 移動速度スライダー / 「接線を表示」トグル
- 接線表示をOFFにして「向きを固定」した比較モード → 進行方向を向かない不自然さを体験

タブB「地形と光」:
- 横から見た2D地形(1次元ハイトマップ y = h(x))。各点の傾き h′(x) から法線を立て、光源方向との内積で明るさを塗る
- 操作: 光の角度スライダー(0〜180°)/ 地形の凸凹の強さスライダー / 「法線を表示」トグル
- 光の角度を動かすと、斜面の明るい面・暗い面がリアルタイムに変わる(朝日〜夕日)

**体験させたいこと**:
- 微分は「速度」だけでなく**「向き・傾き」を取り出す道具**でもあると視野を広げる
- ゲーム画面の陰影が「表面の微分」で出来ていると知る驚き

**まとめ文言案**: 「画面に映るすべての面の明るさは、微分から作った法線で計算されている。あなたが見ているゲーム画面は微分の集合体。」

### フッター

- 6セクションの学びを1行ずつ再掲
- 用語ミニ辞典(微分/積分/接線/法線/Δt/オイラー法/イージング … 各1〜2行)
- 「次に学ぶなら」のリンク集(任意)

---

## 4. 技術仕様

### 4.1 スタック

- **ビルド不要の静的サイト**: HTML + CSS + Vanilla JavaScript(ES Modules)
  - 理由: GitHub Pages でそのまま公開でき、依存更新の保守も不要
- 描画: **Canvas 2D API**(グラフ・CGプレビューとも)。WebGLは使わない(§6の2Dライティングまで Canvas 2D で表現可能)
- 数式表示: 画像・ライブラリを使わず Unicode + CSS で表現(∫, Δ, ′ など)。複雑な数式は出さない方針のため十分
- 外部ライブラリ: **原則ゼロ**(グラフ描画も自前の軽量ルーチン)

### 4.2 ファイル構成

```
/
├─ index.html            … 全セクションのマークアップ
├─ css/
│   └─ style.css         … ダークテーマ、レイアウト、共通UI
├─ js/
│   ├─ main.js           … 各デモの初期化、ナビゲーション
│   ├─ core/
│   │   ├─ graph.js      … グラフ描画エンジン(軸、曲線、接線、塗り、ツールチップ)
│   │   ├─ anim.js       … requestAnimationFrame ループ、再生/停止管理
│   │   └─ controls.js   … スライダー+数値入力の双方向バインド部品
│   └─ demos/
│       ├─ demo1_velocity.js    … §1
│       ├─ demo2_easing.js      … §2
│       ├─ demo3_jump.js        … §3
│       ├─ demo4_riemann.js     … §4
│       ├─ demo5_timestep.js    … §5
│       └─ demo6_tangent_light.js … §6
├─ SPEC.md               … 本仕様書
└─ README.md             … 概要と公開URL
```

### 4.3 非機能要件

| 項目 | 要件 |
|---|---|
| フレームレート | アニメーション中も 60fps 目標(デモは requestAnimationFrame 駆動、非表示セクションは IntersectionObserver で停止) |
| 応答性 | スライダー操作から描画反映まで 1フレーム以内 |
| 対応ブラウザ | 最新の Chrome / Edge / Firefox / Safari(デスクトップ優先) |
| モバイル | 縦積みレイアウトに崩れなく対応。スライダーはタッチ操作可能なサイズ(高さ44px以上) |
| アクセシビリティ | スライダーに label と aria-valuetext、色だけに頼らない表現(線種併用) |
| 言語 | 日本語のみ |
| サイズ | 依存ゼロのため合計 100KB 未満目標 |

### 4.4 公開

- GitHub Pages(main ブランチ / ルート)で公開できる構成にする

---

## 5. 制作の進め方(実装フェーズの計画)

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 1 | 骨組み: index.html + テーマCSS + core/(graph, anim, controls) | 空のデモ枠6つとナビが表示される |
| 2 | §1(速度)と §3(ジャンプ) — 微分・積分の看板デモ | スライダー連動でグラフとプレビューが動く |
| 3 | §4(リーマン和)と §5(タイムステップ) | 収束・誤差が数値とともに見える |
| 4 | §2(イージング)と §6(接線・光) | 全6デモ完成 |
| 5 | 仕上げ: イントロ/フッター、モバイル対応、パフォーマンス確認 | 全要件を満たす |

---

## 6. スコープ外(今回はやらないこと)

- 3D表示(WebGL / Three.js)
- 数式の証明・厳密な定義・練習問題
- 偏微分・テイラー展開など §6 を超える発展的話題(用語辞典で名前だけ触れるのは可)
- 多言語対応、サーバーサイド機能、ユーザーデータ保存
