# 仕様書:『ライティング&レンダリングで体感する微分・積分』インタラクティブ学習ページ

- リポジトリ: `differentialAndintegralWeb`
- バージョン: 2.0(TA/ライティングアーティスト向けに題材を全面改訂)
- 目的の一言まとめ: **「微分・積分って何の役に立つの?」に、ライト・レンダリング・ポストプロセスの実例を触りながら答えるWebページ**

---

## 1. コンセプト

### 1.1 誰のためのページか

- 微分・積分を「習ったけど表面的にしか知らない」TA・ライティングアーティスト・アーティスト職
- 普段触っているパラメータ(トーンカーブ、ノーマルマップ強度、ブルーム半径、サンプル数、フォグ密度、順応速度…)の**数学的な正体**を知りたい人
- 数式の変形よりも「意味」と「使いどころ」を直感で掴みたい人

### 1.2 第一のテーマ

**「便利さの体験」を最優先にする。** 証明や厳密な定義は扱わない。
すべてのセクションで次の3点セットを守る:

1. **ライティング/レンダリングの実務の場面**から始める(カラグレ、ノーマルマップ、ブルーム、ソフトシャドウ、フォグ、自動露出…)
2. **スライダーや数値入力を触ると、グラフとレンダリングプレビューがリアルタイムに動く**
3. 触った結果として「いつも触っているあのパラメータは微分/積分のこれだったのか」と**体で納得**して終わる

### 1.3 貫くストーリー

- **微分 = 「変化・傾きを取り出す」** → コントラスト、法線、エッジはぜんぶ微分
- **積分 = 「細かく刻んで足し合わせる」** → ブラー、ソフトシャドウ、フォグ、フレーム蓄積はぜんぶ積分
- そして**「サンプル数 = 積分の短冊の本数」**という一本の軸が、タップ数・シャドウサンプル・レイマーチのステップ数・TAAの蓄積を全部つなぐ。品質とパフォーマンスのトレードオフの正体は積分誤差である、という着地

### 1.4 デザイン方針

- 1ページ縦スクロール構成。微分編 → 積分編 → 時間編と自然に進む
- 各セクションは「① 実務の場面(2〜3文) → ② インタラクティブデモ → ③ ひとことまとめ」の型で統一
- 数式は最小限。出すときは必ず「日本語の読み下し」を併記する
  - 例: `T = exp(−∫σ dt)` →「透過率は、視線上の濃度を全部足した量で指数的に減る」
- ダークテーマ基調(DCC/ゲームエンジンのUIらしい見た目)。グラフ・プレビューは高コントラストで

---

## 2. ページ全体構成

```
ヘッダー(タイトル + キャッチコピー)
│
├─ §0 イントロ:このページの遊び方
│
├─ 【微分編 — 変化・傾きを取り出す】
│   ├─ §1 トーンカーブの傾き=コントラスト     … カラーグレーディング
│   ├─ §2 ハイトの傾き=法線                    … ノーマルマップとシェーディング
│   └─ §3 画像を微分するとエッジが出る          … アウトライン抽出・ddx/ddy
│
├─ 【積分編 — 刻んで足し合わせる】
│   ├─ §4 重み付きで足す=畳み込み              … ガウシアンブラーとブルーム
│   ├─ §5 光源の面で積分=ソフトシャドウ        … エリアライトとモンテカルロ
│   └─ §6 レイに沿って積分=ボリュームフォグ    … レイマーチング
│
├─ 【時間編 — 描画システムは時間方向にも微積分する】
│   └─ §7 自動露出とフレーム蓄積                … 順応の微分方程式・TAA的蓄積
│
└─ フッター(まとめ + 用語ミニ辞典)
```

- 全7デモ。各デモは独立して動く(順番に読まなくても壊れない)
- ページ上部に固定ナビ(セクションへのジャンプリンク)

---

## 3. 各セクション詳細仕様

> 共通UI仕様:
> - スライダーは操作中もリアルタイム(input イベント)でグラフ・プレビューを更新
> - 数値は現在値をスライダー横に常時表示。数値入力欄と双方向同期
> - 各デモに「リセット」ボタン
> - グラフはマウスホバーで座標値をツールチップ表示

### §0 イントロ

- 内容: 「このページは読むより触るページです」という宣言と、微分・積分の超ざっくり定義
  - 微分 = **変化・傾きを取り出す道具**
  - 積分 = **細かく刻んで足し合わせる道具**
  - 「あなたが今日調整したトーンカーブもブルームもフォグも、中身はこの2つ」という導入
- インタラクション: 背景に小さな装飾デモ(光源がゆっくり動き、地形の陰影とブルームが変わるループアニメ。触れなくてよい、雰囲気づくり)

### §1 トーンカーブの傾き=コントラスト — カラーグレーディング

**場面**: グレーディングでS字カーブを当てるとコントラストが上がり、シャドウを持ち上げると眠くなる。全部知っている現象。ではその「コントラスト」を数値で言うと何か? — **トーンカーブの微分(傾き)**。

**画面構成**(左右2ペイン):
- 左: **プレビュー** — グラデーションランプ+簡単なシーン画像(明暗の分布があるもの)。カーブ適用結果をリアルタイム表示。下にヒストグラム
- 右: **グラフ2段** — 上段: トーンカーブ f(x)(入出力 0〜1)、下段: その微分 f′(x)。f′ > 1 の区間(コントラスト増)をハイライト、f′ < 1 の区間(フラット化)を減light表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| S字の強さ | スライダー | 0〜1 | S字カーブの効き。微分カーブの山谷が連動して変形 |
| ガンマ | スライダー | 0.3〜3.0 | 中間調の持ち上げ/沈み込み |
| リフト(シャドウ) | スライダー | 0〜0.3 | 黒浮き。カーブ低域の傾きが変わる |
| プリセット | ボタン群 | リニア / S字 / フィルム風 / 白飛び気味 | カーブの切替 |
| 「微分カーブを見る」 | トグル | - | 下段グラフの表示ON/OFF |

**体験させたいこと**:
- **傾き > 1 の階調域はコントラストが増え、傾き < 1 の域は眠くなる** — S字が「中間調の傾きを稼ぐために両端の傾きを犠牲にする」形だと分かる
- 白飛び・黒つぶれ = **傾き0(微分がゼロ)で階調情報が死ぬ**、を微分カーブで確認
- ヒストグラムのどの帯域に傾きを配るかがグレーディングの設計だと気づく

**まとめ文言案**: 「コントラストとはトーンカーブの微分のこと。グレーディングは『どの明るさに傾きを配るか』の設計。」

### §2 ハイトの傾き=法線 — ノーマルマップとシェーディング

**場面**: ノーマルマップはなぜ平らなポリゴンに凹凸を見せられるのか。あの青紫のテクスチャの正体は、**ハイトマップを微分して作った法線**。「ノーマルマップの強度」スライダーの数学的な意味もここで分かる。

**画面構成**(縦3段連動):
- 上段: **ハイトマップ h(x)** — 横から見た1次元の断面カーブ
- 中段: **微分 h′(x) と法線ベクトル** — 各点に法線の矢印を立てて表示
- 下段: **シェーディング結果** — 法線と光源方向の内積(ランバート)で明るさを塗った表面。光源アイコンが角度スライダーと連動

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 光の角度 | スライダー | 0〜180° | 朝日〜真上〜夕日。明るい斜面・暗い斜面がリアルタイムに入れ替わる |
| 凹凸の強さ | スライダー | 0〜3 | **ノーマルマップの Strength に相当**。微分のスケール倍率 |
| 凹凸の細かさ | スライダー | 低周波〜高周波 | ディテールの周波数 |
| ハイトマップ編集 | ドラッグ | - | カーブ上の点をドラッグして地形を直接いじれる |
| 「法線を表示」 | トグル | - | 中段の矢印ON/OFF |

**体験させたいこと**:
- ハイトをドラッグすると、微分 → 法線 → 陰影が**3段連動**で変わる因果の連鎖を見る
- 「Strength スライダー = 微分に掛ける倍率」だと分かる(上げすぎると法線が寝て破綻するのも再現)
- 平坦な場所(微分ゼロ)は光を動かしても明るさ変化が乏しい = ディテールが出ないことを確認

**まとめ文言案**: 「ノーマルマップは『微分をテクスチャに焼いたもの』。画面の陰影は表面の微分で出来ている。」

### §3 画像を微分するとエッジが出る — アウトライン抽出・ddx/ddy

**場面**: トゥーン調のアウトラインは、深度バッファや法線バッファを**微分**して「隣のピクセルと大きく違う場所」を探すポストプロセス。GPUが全ピクセルで毎フレーム微分している ddx / ddy(ミップマップ選択にも使われる)にもつなげる。

**画面構成**(左右2ペイン):
- 左: **元バッファ** — 簡単な2Dシーンの疑似深度バッファ(グレースケール。数個の図形が手前/奥に配置)
- 右: **微分結果 → アウトライン合成** — 微分の大きさをそのまま表示するモードと、しきい値で二値化して元画像に輪郭線を合成するモードを切替

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| しきい値 | スライダー | 0〜1 | 線の出る量。下げすぎるとノイズも線になる |
| 線の太さ | スライダー | 1〜5px | 膨張処理 |
| 微分の方向 | 選択 | 横のみ / 縦のみ / 両方(勾配の大きさ) | 横だけだと縦エッジしか出ない、を体験 |
| ソース | 選択 | 深度 / 輝度 | どのバッファを微分するかで線の出方が変わる |
| 図形の配置 | ドラッグ | - | シーン内の図形を動かすとエッジも追従 |

**体験させたいこと**:
- 「エッジ = 微分が大きい場所」。画像処理の微分は**単なる隣のピクセルとの引き算**だと知る(§1の Δt を小さくする話の画像版)
- 横方向だけ微分すると縦線しか出ない → 勾配(全方向の傾き)の必要性を体験
- コラム枠: GPUの ddx/ddy は2×2ピクセルで常に微分していて、UVの微分からミップレベルを選んでいる — 「シェーダーは微分だらけ」

**まとめ文言案**: 「輪郭線は画像の微分。GPUは今この瞬間も全ピクセルで微分している(ddx/ddy)。」

### §4 重み付きで足す=畳み込み — ガウシアンブラーとブルーム

**場面**: ブルームの根っこはブラー。ブラーは「周囲のピクセルを、距離に応じた重みを付けて足し合わせる」= **重み付きの積分(畳み込み)**。タップ数を削ると画質が壊れるのはなぜかもここで分かる。

**画面構成**(縦2段):
- 上段: **カーネルのグラフ** — ガウス関数の連続曲線と、その上に離散タップ(短冊状の棒)を重ね描き。タップの合計(=カーネル下の面積)を数値表示
- 下段: **プレビュー** — 暗いシーンに明るい光源が数個あるHDR風画像。しきい値で明部を抽出 → ブラー → 加算合成(ブルームのフルパイプライン)。ブラー前後の1D輝度断面グラフも併設

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 半径 σ | スライダー | 1〜50px | にじみの広がり。カーネル曲線の幅と連動 |
| タップ数 | スライダー | 3〜64 | **積分の短冊の本数**。少ないとバンディング・欠けが出る |
| ブルームしきい値 | スライダー | 0〜1 | 明部の抽出量 |
| ブルーム強度 | スライダー | 0〜3 | 加算量 |
| 光源の追加/移動 | クリック/ドラッグ | - | プレビュー内の光源を配置し直せる |

**体験させたいこと**:
- カーネル下の**面積が常に1**(重みの合計=1)= 明るさが保存される、を数値で確認。強度を上げるとそこが崩れて「盛る」表現になる
- タップ数スライダーを下げる → カーネルの近似が粗くなり、プレビューにアーティファクトが出る。**タップ数 = 積分の細かさ**、削減はまさに積分誤差との取引だと体験
- σ を上げたのにタップ数据え置きで壊れる、という実務あるあるを再現

**まとめ文言案**: 「ブラーもブルームも積分。タップ数は積分の短冊の本数で、削った分は誤差=アーティファクトになる。」

### §5 光源の面で積分=ソフトシャドウ — エリアライトとモンテカルロ

**場面**: エリアライトの影の縁がふわっとするのは、**光源の面全体からの寄与を積分**しているから。リアルタイムやパストレではこの積分をランダムサンプリング(モンテカルロ)で近似する — レンダラーのノイズとサンプル数の関係の正体。

**画面構成**:
- メイン: **2D断面シーン** — 上部に線分のエリアライト、中央に遮蔽物(ドラッグ可)、下部に地面。地面の各点の明るさ = その点から見えるライトの割合(可視部分の積分)で塗る。本影・半影が自然に現れる
- サブ: **選択した地面1点の詳細** — その点からライトへ伸びるサンプルレイを可視化(遮られたレイは赤、届いたレイは黄)。「届いた本数/全本数」を数値表示
- 補助: 地面に沿った明るさの1Dグラフ(半影部分がなだらかなスロープになる)

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| ライトの大きさ | スライダー | 0(点光源)〜大 | 影がシャープ→ソフトへ連続変化。半影の幅=積分区間の広さ |
| サンプル数 | スライダー | 1〜256 | 少ないと影がザラザラ(ノイズ)、増やすと滑らかに収束 |
| 遮蔽物 | ドラッグ | - | 位置・高さで半影の形が変わる |
| 「毎フレーム乱数を変える」 | トグル | - | ON: レンダラーのノイズのちらつきを再現。OFF: 固定パターン |
| 調査点の選択 | クリック | - | サブビューに表示する地面の点を選ぶ |

**体験させたいこと**:
- **半影 = 積分の値がなだらかに変わる領域**。ライトを大きくする=積分区間が広がる、が影の見た目に直結する
- サンプル数 1 → 256 でノイズが消えていく = **パストレーサーのノイズは積分の途中経過**だと分かる(収束の速さが Ⅴ√n 的に鈍いことも体感される)
- デノイザーやブルーノイズが「なぜ要るのか」の土台がここでできる

**まとめ文言案**: 「影のやわらかさは積分の形。レンダラーのノイズは『積分がまだ足し終わっていない』姿。」

### §6 レイに沿って積分=ボリュームフォグ — レイマーチング

**場面**: フォグやゴッドレイは、**視線(レイ)に沿って霧の濃度を積分**して透過率を出している。実装のレイマーチングは数値積分そのもので、ステップ数を削るとバンディングが出る — §4・§5 と同じ「サンプル数と品質」の構造がここにも。

**画面構成**(左右2ペイン):
- 左: **フォグレンダリング結果** — 2Dシーン(光源+濃度にむらのあるフォグ+遮蔽物)。ゴッドレイ(光芒)が出る
- 右: **1本のレイの解剖図** — 左ビューでクリックしたピクセルのレイを取り出し、レイ上の濃度グラフ+短冊(ステップ)積分を可視化。透過率 T = exp(−∫σ) の値がステップを追うごとに減っていく様子を表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| フォグ濃度 | スライダー | 0〜3 | 全体の濃さ |
| ステップ数 | スライダー | 4〜128 | **積分の細かさ**。少ないとバンディング(縞)が出る |
| 濃度のむら | スライダー | 0〜1 | ノイズによる濃度変化。むらが強いほど少ステップの誤差が目立つ |
| 光源の位置 | ドラッグ | - | ゴッドレイの向きが変わる |
| 調査レイの選択 | クリック | - | 右ビューに解剖するレイを選ぶ |

**体験させたいこと**:
- フォグの1ピクセル = **レイに沿った積分1本**。画面全体では毎フレーム何十万本の積分が走っている
- ステップ数を下げるとバンディングが出る = **バンディングは積分誤差の見た目**だと分かる
- 「濃度が急変する場所ほどステップが要る」= 積分の刻み方の勘所が、フォグ品質調整の勘所と同じだと気づく

**まとめ文言案**: 「フォグの1ピクセルはレイに沿った積分1本。バンディングは積分誤差が可視化されたもの。」

### §7 自動露出とフレーム蓄積 — 時間方向の微積分(描画システム)

**場面**: ここまでは空間の微積分。描画システムは**時間方向**にも微積分している。目の順応(自動露出)は「目標露出へじわっと寄せる」微分方程式。TAAやパストレの蓄積は「フレームをまたいだ積分」。

**画面構成**(タブ切替で2デモ):

タブA「自動露出(順応)」:
- 上: シーンプレビュー(暗い室内 ⇄ 明るい屋外をボタンで切替)。露出が徐々に追いつく
- 下: **露出値の時間グラフ** — 目標値(階段状)と現在値(指数カーブで追従)を重ね描き
- 操作: 「屋外へ出る/室内へ戻る」ボタン / 順応速度スライダー / 「フレームレートを変える」選択(30/60/144fps)
- 見どころ: `E += (target − E) × k × Δt` という毎フレームの1行が、微分方程式 dE/dt = k(target−E) の数値解であること。**Δt を掛け忘れるとfpsで順応速度が変わるバグ**になる、を30fps/144fps切替で実演

タブB「フレーム蓄積(TAA的アキュムレーション)」:
- §5 のノイズだらけのソフトシャドウ画像を毎フレームブレンドして蓄積 → ノイズがみるみる消えていく
- 操作: 蓄積率(ブレンド係数)スライダー / 「リセット」/ 「シーンを動かす」ボタン → 蓄積が破綻してゴースト(残像)が出る
- 見どころ: **蓄積 = 時間方向の積分によるノイズ低減**。動くとゴーストが出る = 時間積分の前提(同じものを足し続ける)が崩れるから。TAAのゴースト問題の正体

**体験させたいこと**:
- 順応・慣性系のパラメータ(adaptation speed など)の裏に微分方程式がいると知る
- 「Δt を掛ける理由」を、fps切替による挙動差で体感する(フレームレート非依存性)
- 空間の積分(§4〜6)で残ったノイズを時間の積分で消す、という**現代レンダリングの全体構造**が見える

**まとめ文言案**: 「順応は時間の微分方程式、TAAは時間の積分。レンダリングは空間でも時間でも微積分している。」

### フッター

- 7セクションの学びを1行ずつ再掲
- **総まとめの一枚図**: 「微分 = コントラスト・法線・エッジ / 積分 = ブラー・影・フォグ・蓄積 / サンプル数 = 積分の短冊の本数」の対応マップ
- 用語ミニ辞典(微分/積分/勾配/法線/畳み込み/モンテカルロ/レイマーチング/ddx・ddy/微分方程式 … 各1〜2行)
- 「次に学ぶなら」のリンク集(任意。レンダリング方程式・PBR・球面調和などの名前だけ紹介)

---

## 4. 技術仕様

### 4.1 スタック

- **ビルド不要の静的サイト**: HTML + CSS + Vanilla JavaScript(ES Modules)
  - 理由: GitHub Pages でそのまま公開でき、依存更新の保守も不要
- 描画: **Canvas 2D API**(グラフ・プレビューとも)。WebGLは使わない
  - ピクセル処理(§3 エッジ、§4 ブラー、§5 影、§6 フォグ)は **低解像度オフスクリーンキャンバス(例: 320×180)に ImageData で書き、拡大表示**してリアルタイム性を確保する
  - 60fpsで間に合わない処理は「パラメータ変更時のみ再計算+結果をキャッシュ」方式に切り替えてよい(§5 のちらつきトグルなど毎フレーム系は解像度をさらに落とす)
- 数式表示: 画像・ライブラリを使わず Unicode + CSS で表現(∫, Δ, ′, σ など)。複雑な数式は出さない方針のため十分
- 外部ライブラリ: **原則ゼロ**(グラフ描画も自前の軽量ルーチン)

### 4.2 ファイル構成

```
/
├─ index.html            … 全セクションのマークアップ
├─ css/
│   └─ style.css         … ダークテーマ、レイアウト、共通UI
├─ js/
│   ├─ main.js           … 各デモの初期化、ナビゲーション
│   ├─ core/
│   │   ├─ graph.js      … グラフ描画エンジン(軸、曲線、接線、塗り、短冊、ツールチップ)
│   │   ├─ anim.js       … requestAnimationFrame ループ、再生/停止管理
│   │   ├─ controls.js   … スライダー+数値入力の双方向バインド部品
│   │   └─ pixels.js     … ImageData ベースの低解像度ピクセル処理ヘルパー
│   └─ demos/
│       ├─ demo1_tonecurve.js   … §1 トーンカーブ
│       ├─ demo2_normal.js      … §2 法線とシェーディング
│       ├─ demo3_edge.js        … §3 エッジ検出
│       ├─ demo4_bloom.js       … §4 畳み込み・ブルーム
│       ├─ demo5_softshadow.js  … §5 ソフトシャドウ・モンテカルロ
│       ├─ demo6_fog.js         … §6 レイマーチング
│       └─ demo7_temporal.js    … §7 自動露出・蓄積
├─ SPEC.md               … 本仕様書
└─ README.md             … 概要と公開URL
```

### 4.3 非機能要件

| 項目 | 要件 |
|---|---|
| フレームレート | アニメーション中も 60fps 目標(デモは requestAnimationFrame 駆動、非表示セクションは IntersectionObserver で停止) |
| 応答性 | スライダー操作から描画反映まで 1フレーム以内(重いピクセル処理は低解像度化で担保) |
| 対応ブラウザ | 最新の Chrome / Edge / Firefox / Safari(デスクトップ優先) |
| モバイル | 縦積みレイアウトに崩れなく対応。スライダーはタッチ操作可能なサイズ(高さ44px以上) |
| アクセシビリティ | スライダーに label と aria-valuetext、色だけに頼らない表現(線種併用) |
| 言語 | 日本語のみ |
| サイズ | 依存ゼロのため合計 150KB 未満目標 |

### 4.4 公開

- GitHub Pages(main ブランチ / ルート)で公開できる構成にする

---

## 5. 制作の進め方(実装フェーズの計画)

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 1 | 骨組み: index.html + テーマCSS + core/(graph, anim, controls, pixels) | 空のデモ枠7つとナビが表示される |
| 2 | §2(法線)と §5(ソフトシャドウ) — 微分・積分の看板デモ | スライダー連動でグラフとプレビューが動く |
| 3 | §1(トーンカーブ)と §4(ブルーム) | 微分カーブ表示・タップ数の劣化が見える |
| 4 | §3(エッジ)と §6(フォグ) | しきい値・ステップ数の効果が見える |
| 5 | §7(時間編)+ 仕上げ: イントロ/フッター、モバイル対応、パフォーマンス確認 | 全要件を満たす |

---

## 6. スコープ外(今回はやらないこと)

- WebGL / Three.js による3D表示(全デモを2D断面・画像処理で表現する)
- 数式の証明・厳密な定義・練習問題
- レンダリング方程式の一般形、BRDF/PBRの詳細、球面調和など §7 を超える発展的話題(フッターで名前だけ触れるのは可)
- 多言語対応、サーバーサイド機能、ユーザーデータ保存
