# 仕様書:『ライティング&レンダリングで体感する微分・積分』インタラクティブ学習ページ

- リポジトリ: `differentialAndintegralWeb`
- バージョン: 3.0(中学数学だけで読めるよう難易度設計を全面導入)
- 目的の一言まとめ: **中学数学しか知らなくても、ライト・レンダリング・ポストプロセスの実例を触りながら「微分・積分の便利さ」が分かるWebページ**

---

## 1. コンセプト

### 1.1 誰のためのページか

- **数学の前提知識は中学数学まで**(一次関数の傾き、長方形の面積、割合、負の数)。高校数学は仮定しない
- 微分・積分を「言葉は知っているが中身は知らない」TA・ライティングアーティスト・アーティスト職
- 普段触っているパラメータ(トーンカーブ、ノーマルマップ強度、ブルーム半径、サンプル数、フォグ密度、順応速度…)の**数学的な正体**を知りたい人

### 1.2 第一のテーマ

**「便利さの体験」を最優先にする。** 証明や厳密な定義は扱わない。
すべてのセクションで次の3点セットを守る:

1. **ライティング/レンダリングの実務の場面**から始める
2. **スライダーや数値入力を触ると、グラフとプレビューがリアルタイムに動く**
3. 「いつも触っているあのパラメータは微分/積分のこれだったのか」と**体で納得**して終わる

### 1.3 難易度設計(v3.0の柱)

**「中学数学 → 微分・積分」への橋を、ページの中で全部架ける。** 外部知識を要求しない。

- **準備編を新設**: 微分の入口(傾き)と積分の入口(面積)を、中学数学の復習から始める2つのミニデモで先に体験させる
- **易しい順に並べる**: 各セクションに難易度バッジ(★1〜★4)を付け、★1から順に配置。後のセクションは前のセクションの考え方だけを使う
- **各セクション冒頭に「つかう数学」ボックス**: そのデモで使う数学を中学カリキュラムの言葉で明示する
  - 例: 「一次関数の傾き(中2)/ 長方形の面積(小学校)/ 割合(中1)」
- **高校以上の道具は本文に出さない**。出すなら折りたたみの「もっと知りたい人へ」ボックスに隔離する
  - 使わない言葉: 極限、lim、内積、指数関数 exp、微分方程式(正式な扱いとして)
  - 言い換える: 内積 →「面が光の方を向いているほど明るい」/ exp →「1歩ごとに◯%ずつ減る掛け算の積み重ね(複利と同じ)」/ 微分方程式 →「毎フレーム、目標との差の一定割合だけ近づく計算」
- **記号は最小限**: dx/dt や ∫ は「読み方の紹介」として出すだけで、操作や理解には使わせない。すべて日本語の読み下しを主役にする

### 1.4 貫くストーリー

- **微分 = 「傾き(変化の速さ)を取り出す」** → コントラスト、法線、エッジはぜんぶ傾き
- **積分 = 「細かく刻んで足し合わせる」** → 露出、ブラー、影、フォグ、フレーム蓄積はぜんぶ足し算
- **「サンプル数 = 刻みの細かさ」**という一本の軸が、タップ数・シャドウサンプル・レイマーチのステップ数を全部つなぐ。品質とパフォーマンスのトレードオフの正体は「足し算の刻みの粗さ」である、という着地

### 1.5 デザイン方針

- 1ページ縦スクロール構成。準備編 → 微分編 → 積分編 → 挑戦編と、難易度順に自然に進む
- 各セクションは「① つかう数学ボックス → ② 実務の場面(2〜3文) → ③ インタラクティブデモ → ④ ひとことまとめ」の型で統一
- ダークテーマ基調(DCC/ゲームエンジンのUIらしい見た目)。グラフ・プレビューは高コントラストで

---

## 2. ページ全体構成

```
ヘッダー(タイトル + 「中学数学だけでOK」の宣言)
│
├─ §0 イントロ:このページの遊び方
│
├─ 【準備編 — 中学数学から橋を架ける】
│   ├─ P1 傾き=変化の速さ ★1      … ライトのフェードイン(微分の入口)
│   └─ P2 面積=積み重ねの合計 ★1  … カメラの露出(積分の入口)
│
├─ 【微分編 — 傾きを取り出す】
│   ├─ §1 トーンカーブの傾き=コントラスト ★2 … カラーグレーディング
│   ├─ §2 ハイトの傾き=法線 ★2               … ノーマルマップとシェーディング
│   └─ §3 隣との差=エッジ ★2                 … アウトライン抽出
│
├─ 【積分編 — 刻んで足し合わせる】
│   ├─ §4 重み付きで足す=ブラーとブルーム ★3
│   ├─ §5 ランダムに試して足す=ソフトシャドウ ★3
│   └─ §6 レイに沿って足す=ボリュームフォグ ★3
│
├─ 【挑戦編 — 時間方向の微積分】
│   └─ §7 自動露出とフレーム蓄積 ★4
│
└─ フッター(まとめの一枚図 + 用語ミニ辞典)
```

- 準備編2 + 本編7 = 全9デモ。各デモは独立して動くが、概念は前から順に積み上がる
- ページ上部に固定ナビ(セクションへのジャンプリンク+難易度バッジ表示)

---

## 3. 各セクション詳細仕様

> 共通UI仕様:
> - スライダーは操作中もリアルタイム(input イベント)でグラフ・プレビューを更新
> - 数値は現在値をスライダー横に常時表示。数値入力欄と双方向同期
> - 各デモに「リセット」ボタン
> - グラフはマウスホバーで座標値をツールチップ表示
> - 「もっと知りたい人へ」は折りたたみ(details/summary)。閉じたままでも本編は完結する

### §0 イントロ

- 内容:
  - 「このページは読むより触るページです」「必要な数学は中学までで全部です」の宣言
  - 微分 = **傾き(変化の速さ)を取り出す道具**、積分 = **細かく刻んで足し合わせる道具**、の2行だけ先に見せる
  - 「あなたが今日調整したトーンカーブもブルームもフォグも、中身はこの2つ」という導入
- インタラクション: 背景に小さな装飾デモ(光源がゆっくり動き、地形の陰影とブルームが変わるループアニメ。触れなくてよい)

### P1 傾き=変化の速さ ★1 — ライトのフェードイン(微分の入口)

> **つかう数学**: 一次関数 y = ax + b の傾き(中2)、負の数(中1)

**場面**: 照明卓のフェーダーを上げてライトをフェードインさせる。「1秒あたりどれだけ明るくなるか」— この**変化の速さ**こそ、微分で取り出せるもの。

**画面構成**(左右2ペイン):
- 左: **プレビュー** — 部屋のシーン。ライトの明るさが時間とともに変わる
- 右: **グラフ** — 横軸: 時間、縦軸: 明るさ。現在時刻の点と、その点での傾きを示す矢印(接線)を表示。「今の変化の速さ: +20/秒」のように日本語で数値表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 時間 | スライダー | 0〜5s | プレビューとグラフ上の点・傾き矢印が連動 |
| 再生/停止 | ボタン | - | 時間を自動で進める |
| フェードの形 | 選択 | 一定の速さ(直線)/ だんだんゆっくり / ゆっくり始まりゆっくり終わる / 一瞬明るくして戻す | グラフの形を切替 |
| 直線の傾き | スライダー | -50〜+50 | 「一定の速さ」選択時のみ。中2の y = ax の a をそのまま触る |

**体験させたいこと**:
- まず**直線**で「傾き = 1秒あたりの変化量」という中2の知識を確認する(既知の安心からスタート)
- 次に**カーブ**に切り替えると、傾きが場所ごとに違う → 「曲線の各点での傾きを求めること、それが微分」とここで初めて言う
- 「一瞬明るくして戻す」では、ピークで傾き0・下りで傾きマイナスになるのを見る(負の数の復習)

**まとめ文言案**: 「微分とは『グラフの各点での傾き』のこと。直線なら中2でやった a、曲線なら場所ごとに変わる a。それだけ。」

### P2 面積=積み重ねの合計 ★1 — カメラの露出(積分の入口)

> **つかう数学**: 長方形の面積 = 縦×横(小学校)、たし算

**場面**: カメラの露出は「シャッターが開いている間に入ってきた光の合計」。明るさが一定なら **明るさ × 時間 = 長方形の面積** で計算できる。では撮影中に明るさが変わったら(ネオンのちらつき、雲の流れる空)?

**画面構成**(上下2段):
- 上: **グラフ** — 横軸: 時間、縦軸: シーンの明るさ。シャッターが開いている区間の下側を短冊(細い長方形)で塗る。短冊の合計面積を「取り込んだ光の量」として数値表示
- 下: **プレビュー** — その光の量で決まる「撮れた写真」の明るさ。面積の数値と写真の明るさが連動

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| シャッター時間 | スライダー | 0.1〜4s | 塗られる区間の幅。長いほど明るく写る |
| シーンの明るさ | 選択 | 一定 / ちらつくネオン / 雲が流れる空 | グラフの形を切替 |
| 短冊の本数 | スライダー | 1〜200 | 刻みの細かさ。粗いと面積の数値が不正確、細かくすると本当の値に近づいていく |

**体験させたいこと**:
- 「一定」ならただの長方形(小学校の面積)。**積分は面積計算の延長**だと最初に安心させる
- 明るさが変化するカーブでは、**細い長方形に刻んで足せばいい**と分かる。短冊スライダーで「刻むほど正確」を数値で体験
- 「写真の明るさ」という実感のある結果と面積の数値が一致する納得感

**まとめ文言案**: 「積分とは『細かく刻んで全部足す』こと。長方形の面積計算を、形が曲がっていても使えるようにしただけ。カメラは毎ショットこれをやっている。」

### §1 トーンカーブの傾き=コントラスト ★2 — カラーグレーディング

> **つかう数学**: P1の「グラフの各点での傾き」だけ

**場面**: グレーディングでS字カーブを当てるとコントラストが上がり、シャドウを持ち上げると眠くなる。全部知っている現象。その「コントラスト」を数値で言うと何か? — **トーンカーブの傾き**。

**画面構成**(左右2ペイン):
- 左: **プレビュー** — グラデーションランプ+明暗の分布がある簡単なシーン画像。カーブ適用結果をリアルタイム表示。下にヒストグラム
- 右: **グラフ2段** — 上段: トーンカーブ(横軸: 元の明るさ、縦軸: 出力の明るさ)。下段: 各点での傾きのグラフ。傾き > 1 の区間(コントラスト増)を強調表示、傾き < 1 の区間(眠くなる)を淡色表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| S字の強さ | スライダー | 0〜1 | 傾きグラフの山谷が連動して変形 |
| 明るさの中心(ガンマ相当) | スライダー | 低め〜高め | 中間調の持ち上げ/沈み込み(「ガンマ」の語は補足での紹介に留める) |
| リフト(シャドウ) | スライダー | 0〜0.3 | 黒浮き。カーブ低域の傾きが変わる |
| プリセット | ボタン群 | 変化なし / S字 / フィルム風 / 白飛び気味 | カーブの切替 |
| 「傾きグラフを見る」 | トグル | - | 下段グラフの表示ON/OFF |

**体験させたいこと**:
- **傾き > 1 の明るさ帯はコントラストが増え、傾き < 1 の帯は眠くなる** — S字は「中間調の傾きを稼ぐために両端の傾きを犠牲にする」形だと分かる
- 白飛び・黒つぶれ = **傾き0で階調の差が消える**、を傾きグラフで確認
- ヒストグラムのどの帯域に傾きを配るかがグレーディングの設計だと気づく

**まとめ文言案**: 「コントラストとはトーンカーブの傾きのこと。グレーディングは『どの明るさに傾きを配るか』の設計。」

### §2 ハイトの傾き=法線 ★2 — ノーマルマップとシェーディング

> **つかう数学**: P1の「傾き」+ 「面が光の方を向いているほど明るい」という日常感覚

**場面**: ノーマルマップはなぜ平らなポリゴンに凹凸を見せられるのか。あの青紫のテクスチャの正体は、**ハイトマップの傾きから作った「面の向き(法線)」**。「ノーマルマップの強度」スライダーの意味もここで分かる。

**画面構成**(縦3段連動):
- 上段: **ハイトマップ** — 横から見た1次元の断面カーブ(地形の高さ)
- 中段: **傾きと法線** — 各点の傾きの値と、傾きに直角に立てた法線の矢印を表示
- 下段: **シェーディング結果** — 「法線が光の方を向いているほど明るい」ルールで塗った表面。光源アイコンが角度スライダーと連動

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 光の角度 | スライダー | 0〜180° | 朝日〜真上〜夕日。明るい斜面・暗い斜面がリアルタイムに入れ替わる |
| 凹凸の強さ | スライダー | 0〜3 | **ノーマルマップの Strength に相当**。傾きに掛ける倍率 |
| 凹凸の細かさ | スライダー | 低周波〜高周波 | ディテールの周波数 |
| ハイトマップ編集 | ドラッグ | - | カーブ上の点をドラッグして地形を直接いじれる |
| 「法線を表示」 | トグル | - | 中段の矢印ON/OFF |

**体験させたいこと**:
- ハイトをドラッグすると、傾き → 法線 → 陰影が**3段連動**で変わる因果の連鎖を見る
- 「Strength スライダー = 傾きに掛ける倍率」だと分かる(上げすぎると法線が寝て破綻するのも再現)
- 平坦な場所(傾き0)は光を動かしても明るさ変化が乏しい = ディテールが出ないことを確認
- 「もっと知りたい人へ」: 明るさの計算の正体(角度と明るさの関係。高校で「内積」という道具になる、と名前だけ紹介)

**まとめ文言案**: 「ノーマルマップは『傾きをテクスチャに焼いたもの』。画面の陰影は表面の傾き=微分で出来ている。」

### §3 隣との差=エッジ ★2 — アウトライン抽出

> **つかう数学**: 引き算だけ(P1の傾きが「隣との差」になる話)

**場面**: トゥーン調のアウトラインは、深度バッファ(カメラからの距離を濃淡にした画像)で**隣のピクセルとの差**が大きい場所を探すポストプロセス。実は「差」は傾きの親戚 — 画像の世界の微分。

**画面構成**(左右2ペイン):
- 左: **元バッファ** — 簡単な2Dシーンの疑似深度バッファ(グレースケール。数個の図形が手前/奥に配置)
- 右: **差の大きさ → アウトライン合成** — 隣との差をそのまま濃淡表示するモードと、しきい値で線にして元画像に合成するモードを切替

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| しきい値 | スライダー | 0〜1 | 線の出る量。下げすぎるとノイズも線になる |
| 線の太さ | スライダー | 1〜5px | 膨張処理 |
| 差をとる方向 | 選択 | 横のみ / 縦のみ / 両方 | 横だけだと縦エッジしか出ない、を体験 |
| ソース | 選択 | 深度 / 明るさ | どの画像の差をとるかで線の出方が変わる |
| 図形の配置 | ドラッグ | - | シーン内の図形を動かすとエッジも追従 |

**体験させたいこと**:
- 「エッジ = 隣との差が大きい場所」。画像の微分は**ただの引き算**だと知る(拍子抜けさせるのが狙い)
- P1の傾きとのつながり: グラフの傾きも「隣の点との差 ÷ 間隔」だった。画像は間隔が1ピクセルなだけ
- 横方向だけ差をとると縦線しか出ない → 両方向を見る必要性を体験
- 「もっと知りたい人へ」: GPUは ddx/ddy という命令で常に隣接ピクセルとの差を計算していて、テクスチャのミップ選択に使っている

**まとめ文言案**: 「輪郭線は画像の微分=隣のピクセルとの引き算。GPUは今この瞬間も全ピクセルで微分している。」

### §4 重み付きで足す=ブラーとブルーム ★3 — ガウシアンブラー

> **つかう数学**: P2の「刻んで足す」+ 割合(中1)

**場面**: ブルームの根っこはブラー。ブラーは「周囲のピクセルを、近いほど大きい割合(重み)を付けて混ぜ合わせる」計算。P2の「刻んで足す」に**重み**が付いただけ。タップ数を削ると画質が壊れる理由もここで分かる。

**画面構成**(縦2段):
- 上段: **重みのグラフ** — 釣鐘型の重み曲線と、その上に離散タップ(短冊状の棒)を重ね描き。「重みの合計 = 100%」を数値表示
- 下段: **プレビュー** — 暗いシーンに明るい光源が数個ある画像。明部抽出 → ブラー → 加算合成(ブルームのフルパイプライン)。ブラー前後の明るさ断面グラフも併設

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| 半径 | スライダー | 1〜50px | にじみの広がり。重み曲線の幅と連動 |
| タップ数 | スライダー | 3〜64 | **P2の短冊の本数と同じもの**。少ないと縞や欠けが出る |
| ブルームしきい値 | スライダー | 0〜1 | 明部の抽出量 |
| ブルーム強度 | スライダー | 0〜3 | 加算量 |
| 光源の追加/移動 | クリック/ドラッグ | - | プレビュー内の光源を配置し直せる |

**体験させたいこと**:
- 重みの**合計が100%**なら全体の明るさが変わらない(混ぜているだけ)、を数値で確認。強度を上げるとそこが崩れて「盛る」表現になる
- タップ数スライダーを下げる → プレビューにアーティファクト。**タップ数 = P2の短冊の本数**、削減は精度との取引だと体験
- 半径を上げたのにタップ数据え置きで壊れる、という実務あるあるを再現

**まとめ文言案**: 「ブラーもブルームも『重み付きで刻んで足す』=積分。タップ数は短冊の本数で、削った分は誤差=アーティファクトになる。」

### §5 ランダムに試して足す=ソフトシャドウ ★3 — エリアライトとモンテカルロ

> **つかう数学**: P2の「刻んで足す」+ 割合(中1)。「ランダムに試して割合で近似する」という新しい考え方をここで導入

**場面**: エリアライトの影の縁がふわっとするのは、地面の各点が「光源の面のどれだけの部分から光をもらえるか」の**合計(積分)**で明るさが決まるから。レンダラーはこれを「ランダムに何本か試して、届いた割合で近似」する — ノイズとサンプル数の関係の正体。

**画面構成**:
- メイン: **2D断面シーン** — 上部に線分のエリアライト、中央に遮蔽物(ドラッグ可)、下部に地面。地面の各点の明るさ = その点から見えるライトの割合で塗る。本影・半影が自然に現れる
- サブ: **選択した地面1点の詳細** — その点からライトへ伸びるサンプルの線を可視化(遮られた線は赤、届いた線は黄)。「届いた本数 ÷ 全本数 = 明るさ」を数値表示
- 補助: 地面に沿った明るさのグラフ(半影部分がなだらかなスロープになる)

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| ライトの大きさ | スライダー | 0(点光源)〜大 | 影がシャープ→ソフトへ連続変化 |
| サンプル数 | スライダー | 1〜256 | 少ないと影がザラザラ(ノイズ)、増やすと滑らかに |
| 遮蔽物 | ドラッグ | - | 位置・高さで半影の形が変わる |
| 「毎フレーム乱数を変える」 | トグル | - | ON: レンダラーのノイズのちらつきを再現。OFF: 固定パターン |
| 調査点の選択 | クリック | - | サブビューに表示する地面の点を選ぶ |

**体験させたいこと**:
- **半影 = 「見える割合」がなだらかに変わる領域**。ライトを大きくする=足し合わせる範囲が広がる、が影の見た目に直結する
- 「届いた本数÷全本数」というだけの計算でリアルな影が出る驚き。サンプル数 1 → 256 でノイズが消えていく = **パストレーサーのノイズは足し算の途中経過**だと分かる
- デノイザーが「なぜ要るのか」の土台がここでできる

**まとめ文言案**: 「影のやわらかさは積分の形。レンダラーのノイズは『まだ足し終わっていない積分』の姿。」

### §6 レイに沿って足す=ボリュームフォグ ★3 — レイマーチング

> **つかう数学**: P2の「刻んで足す」+ 割合の掛け算(「毎歩◯%ずつ減る」)

**場面**: フォグやゴッドレイは、**視線(レイ)に沿って霧の濃さを少しずつ足しながら進む**ことで「どれだけ光が届くか」を計算している。実装のレイマーチングは「刻んで足す」そのもので、ステップ数を削るとバンディング(縞)が出る — §4・§5 と同じ構造がここにも。

**画面構成**(左右2ペイン):
- 左: **フォグレンダリング結果** — 2Dシーン(光源+濃度にむらのあるフォグ+遮蔽物)。ゴッドレイ(光芒)が出る
- 右: **1本のレイの解剖図** — 左ビューでクリックしたピクセルのレイを取り出し、レイ上の濃度グラフ+短冊(ステップ)を可視化。「残っている光」が1歩ごとに◯%ずつ減っていく様子を棒グラフで表示

**操作**:
| コントロール | 種類 | 範囲 | 効果 |
|---|---|---|---|
| フォグ濃度 | スライダー | 0〜3 | 全体の濃さ |
| ステップ数 | スライダー | 4〜128 | **刻みの細かさ**。少ないとバンディング(縞)が出る |
| 濃度のむら | スライダー | 0〜1 | むらが強いほど少ステップの誤差が目立つ |
| 光源の位置 | ドラッグ | - | ゴッドレイの向きが変わる |
| 調査レイの選択 | クリック | - | 右ビューに解剖するレイを選ぶ |

**体験させたいこと**:
- フォグの1ピクセル = **レイに沿った「刻んで足す」計算1本**。画面全体では毎フレーム何十万本も走っている
- 「残る光は毎歩◯%ずつ減る」= 割合の掛け算の積み重ね(お金の複利と同じ形)という直感を得る
- ステップ数を下げるとバンディングが出る = **バンディングは刻みの粗さ(積分誤差)の見た目**だと分かる
- 「もっと知りたい人へ」: この「毎歩◯%減」を極限まで細かくしたものが高校で習う指数関数 e に繋がる、と名前だけ紹介

**まとめ文言案**: 「フォグの1ピクセルはレイに沿った積分1本。バンディングは積分誤差が可視化されたもの。」

### §7 自動露出とフレーム蓄積 ★4 — 時間方向の微積分(挑戦編)

> **つかう数学**: これまでの全部(傾き、刻んで足す、割合)。新しい数学は出てこないが、組み合わせて使う

**場面**: ここまでは画面の中(空間)の微積分。描画システムは**時間方向**にも微積分している。目の順応(自動露出)は「目標の明るさへ毎フレーム少しずつ近づく」計算、TAAやパストレの蓄積は「フレームをまたいで足し合わせる」計算。

**画面構成**(タブ切替で2デモ):

タブA「自動露出(順応)」:
- 上: シーンプレビュー(暗い室内 ⇄ 明るい屋外をボタンで切替)。露出が徐々に追いつく
- 下: **露出値の時間グラフ** — 目標値(階段状)と現在値(なめらかに追いつくカーブ)を重ね描き
- 操作: 「屋外へ出る/室内へ戻る」ボタン / 順応速度スライダー / フレームレート選択(30/60/144fps)
- 見どころ: 「毎フレーム、目標との差の◯%だけ近づく」というたった1行の計算がこのカーブを生む。**Δt(1フレームの時間)を掛け忘れると、fpsによって順応の速さが変わるバグ**になる、を30fps/144fps切替で実演
- 「もっと知りたい人へ」: この「差に比例して変化する」型の計算は、大学で「微分方程式」と呼ばれる道具の入口

タブB「フレーム蓄積(TAA的アキュムレーション)」:
- §5 のノイズだらけのソフトシャドウ画像を毎フレーム混ぜ合わせて蓄積 → ノイズがみるみる消えていく
- 操作: 蓄積率(混ぜる割合)スライダー / 「リセット」/ 「シーンを動かす」ボタン → 蓄積が破綻してゴースト(残像)が出る
- 見どころ: **蓄積 = 時間方向の「刻んで足す」によるノイズ低減**。動くとゴーストが出る = 「同じものを足し続ける」前提が崩れるから。TAAのゴースト問題の正体

**体験させたいこと**:
- 順応・慣性系のパラメータ(adaptation speed など)の裏にある「毎フレームの小さな計算」を見る
- 「Δt を掛ける理由」を、fps切替による挙動差で体感する(フレームレート非依存性)
- 空間の足し算(§4〜6)で残ったノイズを時間の足し算で消す、という**現代レンダリングの全体構造**が見える

**まとめ文言案**: 「順応は時間方向の微分の応用、蓄積は時間方向の積分。レンダリングは空間でも時間でも微積分している。」

### フッター

- 全9セクションの学びを1行ずつ再掲
- **総まとめの一枚図**: 「微分 = 傾き = コントラスト・法線・エッジ / 積分 = 刻んで足す = 露出・ブラー・影・フォグ・蓄積 / サンプル数 = 刻みの細かさ」の対応マップ
- 用語ミニ辞典(微分/積分/傾き/法線/重み/サンプル/レイマーチング/Δt … 各1〜2行、すべて中学数学の言葉で)
- 「次に学ぶなら」のリンク集(任意。高校数学のどの単元がどのデモに対応するかの案内)

---

## 4. 技術仕様

### 4.1 スタック

- **ビルド不要の静的サイト**: HTML + CSS + Vanilla JavaScript(ES Modules)
  - 理由: GitHub Pages でそのまま公開でき、依存更新の保守も不要
- 描画: **Canvas 2D API**(グラフ・プレビューとも)。WebGLは使わない
  - ピクセル処理(§3 エッジ、§4 ブラー、§5 影、§6 フォグ)は **低解像度オフスクリーンキャンバス(例: 320×180)に ImageData で書き、拡大表示**してリアルタイム性を確保する
  - 60fpsで間に合わない処理は「パラメータ変更時のみ再計算+結果をキャッシュ」方式に切り替えてよい
- 数式表示: 画像・ライブラリを使わず Unicode + CSS で表現(∫, Δ など)。記号は紹介に留める方針のため十分
- 外部ライブラリ: **原則ゼロ**(グラフ描画も自前の軽量ルーチン)

### 4.2 ファイル構成

```
/
├─ index.html            … 全セクションのマークアップ
├─ css/
│   └─ style.css         … ダークテーマ、レイアウト、共通UI、難易度バッジ
├─ js/
│   ├─ main.js           … 各デモの初期化、ナビゲーション
│   ├─ core/
│   │   ├─ graph.js      … グラフ描画エンジン(軸、曲線、接線、塗り、短冊、ツールチップ)
│   │   ├─ anim.js       … requestAnimationFrame ループ、再生/停止管理
│   │   ├─ controls.js   … スライダー+数値入力の双方向バインド部品
│   │   └─ pixels.js     … ImageData ベースの低解像度ピクセル処理ヘルパー
│   └─ demos/
│       ├─ prep1_slope.js       … P1 傾き(フェードイン)
│       ├─ prep2_area.js        … P2 面積(露出)
│       ├─ demo1_tonecurve.js   … §1 トーンカーブ
│       ├─ demo2_normal.js      … §2 法線とシェーディング
│       ├─ demo3_edge.js        … §3 エッジ検出
│       ├─ demo4_bloom.js       … §4 ブラー・ブルーム
│       ├─ demo5_softshadow.js  … §5 ソフトシャドウ
│       ├─ demo6_fog.js         … §6 レイマーチング
│       └─ demo7_temporal.js    … §7 自動露出・蓄積
├─ SPEC.md               … 本仕様書
└─ README.md             … 概要と公開URL
```

### 4.3 非機能要件

| 項目 | 要件 |
|---|---|
| フレームレート | アニメーション中も 60fps 目標(デモは requestAnimationFrame 駆動、非表示セクションは IntersectionObserver で停止) |
| 応答性 | スライダー操作から描画反映まで 1フレーム以内(重いピクセル処理は低解像度化で担保) |
| 対応ブラウザ | 最新の Chrome / Edge / Firefox / Safari(デスクトップ優先) |
| モバイル | 縦積みレイアウトに崩れなく対応。スライダーはタッチ操作可能なサイズ(高さ44px以上) |
| アクセシビリティ | スライダーに label と aria-valuetext、色だけに頼らない表現(線種併用) |
| 言語 | 日本語のみ。**本文の数学用語は中学カリキュラム内に限定**(逸脱は折りたたみ内のみ) |
| サイズ | 依存ゼロのため合計 150KB 未満目標 |

### 4.4 公開

- GitHub Pages(main ブランチ / ルート)で公開できる構成にする

---

## 5. 制作の進め方(実装フェーズの計画)

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 1 | 骨組み: index.html + テーマCSS + core/(graph, anim, controls, pixels) | 空のデモ枠9つとナビが表示される |
| 2 | **P1(傾き)と P2(面積) — 全体の土台になる準備編を最初に作り込む** | 中学数学からの橋がデモとして成立している |
| 3 | §1(トーンカーブ)と §2(法線) | 傾きの応用2連が動く |
| 4 | §3(エッジ)と §4(ブルーム) | 差分と畳み込みが動く |
| 5 | §5(ソフトシャドウ)と §6(フォグ) | サンプル数と品質の関係が見える |
| 6 | §7(時間編)+ 仕上げ: イントロ/フッター、モバイル対応、パフォーマンス確認 | 全要件を満たす |

---

## 6. スコープ外(今回はやらないこと)

- WebGL / Three.js による3D表示(全デモを2D断面・画像処理で表現する)
- 数式の証明・厳密な定義・練習問題
- 高校以上の数学の本格的な導入(極限、三角関数、指数関数、微分方程式は「もっと知りたい人へ」での名前紹介まで)
- 多言語対応、サーバーサイド機能、ユーザーデータ保存
